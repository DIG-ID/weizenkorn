# Auto detect text files and force LF line endings on checkout,
# regardless of each dev's local core.autocrlf setting.
* text=auto eol=lf

# Excluded from the theme zip (npm run zip / release workflow via git archive).
# dist/ is regenerated by the first build; the rest is starter-repo housekeeping.
dist/ export-ignore
TODO.md export-ignore
.github/ export-ignore
